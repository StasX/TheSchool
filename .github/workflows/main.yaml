name: main

on:
  push:
    branches:
      - dev
      - main

  pull_request:
    branches:
      - dev
      - main

jobs:
  checkov:
    name: Checkov Docker Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run Checkov on server dockerfile
        uses: bridgecrewio/checkov-action@master
        with:
          file: server/Dockerfile
          framework: dockerfile
          quiet: true
          output_format: junitxml
          output_file_path: checkov-results.xml,

      - name: Upload Checkov report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: checkov-results
          path: checkov-results.xml
          if-no-files-found: warn

  phpunit:
    name: PHPUnit
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: server

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.5'
          extensions: mbstring, pdo_sqlite, sqlite3, fileinfo
          coverage: none

      - name: Get Composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> "$GITHUB_OUTPUT"

      - name: Cache Composer dependencies
        uses: actions/cache@v5
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('server/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-

      - name: Install Composer dependencies
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: Prepare environment
        run: |
          cp .env.example .env
          php artisan key:generate

      - name: Run PHPUnit
        run: php artisan test --log-junit storage/logs/phpunit.xml

      - name: Upload PHPUnit report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: phpunit-results
          path: server/storage/logs/phpunit.xml
          if-no-files-found: warn

  frontend:
    name: Frontend Build
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: server

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: server/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

  cypress:
    name: Cypress E2E
    runs-on: ubuntu-latest
    needs:
      - checkov
      - phpunit
      - frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm

      - name: Install Cypress dependencies
        run: npm ci

      - name: Prepare environment
        run: cp server/.env.example .env

      - name: Start application
        run: docker compose up -d --build

      - name: Wait for application
        run: |
          set -a
          source .env
          set +a

          for i in {1..30}; do
            if curl --fail "$APP_URL" > /dev/null 2>&1; then
              exit 0
            fi

            echo "Waiting for application..."
            sleep 5
          done

          echo "Application did not start"
          docker compose logs
          exit 1

      - name: Run Cypress
        run: |
          set -a
          source .env
          set +a
          npm run cypress:run

      - name: Save Docker logs
        if: failure()
        run: docker compose logs > docker.log 2>&1

      - name: Upload Docker logs
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: docker-logs
          path: docker.log
          if-no-files-found: ignore

      - name: Upload Cypress screenshots
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: cypress-screenshots
          path: cypress/screenshots
          if-no-files-found: ignore

      - name: Upload Cypress videos
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: cypress-videos
          path: cypress/videos
          if-no-files-found: ignore

      - name: Stop application
        if: always()
        run: docker compose down -v