<?xml version="1.0"?>
<ruleset
    name="TheSchool"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:noNamespaceSchemaLocation="https://schema.phpcodesniffer.com/phpcs.xsd"
>
    <description>PHP_CodeSniffer configuration for the TheSchool project.</description>

    <!-- Standard -->
    <rule ref="PSR12" />

    <rule ref="PSR1.Methods.CamelCapsMethodName">
        <exclude-pattern>tests/*</exclude-pattern>
    </rule>

    <rule ref="PSR1.Files.SideEffects.FoundWithSymbols">
        <exclude-pattern>public/index.php</exclude-pattern>
    </rule>

    <!-- Project directories -->
    <file>app</file>
    <file>bootstrap</file>
    <file>config</file>
    <file>public</file>
    <file>tests</file>

    <!-- Ignore -->
    <exclude-pattern>vendor/*</exclude-pattern>
    <exclude-pattern>storage/*</exclude-pattern>
    <exclude-pattern>public/css/*</exclude-pattern>
    <exclude-pattern>public/js/*</exclude-pattern>
    <exclude-pattern>bootstrap/cache/*</exclude-pattern>

    <!-- File extensions -->
    <arg name="extensions" value="php" />

    <!-- Display -->
    <arg value="sp" />
    <arg name="colors" />
    <arg name="parallel" value="8" />

    <!-- PHP version -->
    <config name="php_version" value="80500" />

    <!-- Encoding -->
    <config name="encoding" value="utf-8" />
</ruleset>
